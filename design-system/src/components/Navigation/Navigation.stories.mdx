import { Meta, Preview, Story, ArgsTable } from "@storybook/addon-docs/blocks";

import Navigation from ".";
import Avatar from "../Avatar";
import Button from "../Button";
import Icon from "../Icon";

import avatarImg from "../../../../product/src/assets/avatar.png";

<Meta title="Components/Navigation" />

## Navigation

Navigation component

<Preview>
    <Story name="With all components">
        <Navigation
            prependChild={<Avatar src={avatarImg} />}
            label="Navigation"
            appendChild={
                <Button isOutline>
                    <Icon name="menu" />
                </Button>
            }
        />
    </Story>
    <Story name="Prepend component and title">
        <Navigation
            prependChild={<Avatar src={avatarImg} />}
            label="Navigation"
        />
    </Story>
    <Story name="Without label">
        <Navigation
            prependChild={<Avatar src={avatarImg} />}
            appendChild={
                <Button isOutline>
                    <Icon name="menu" />
                </Button>
            }
        />
    </Story>
</Preview>

## Props

<ArgsTable of={Navigation} />
